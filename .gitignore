# Brainstorm visual companion (superpowers) — local, do not commit
.superpowers/

# OS noise
.DS_Store
Thumbs.db

# Host build output
/build/

# Stray compiled artifacts (e.g. ad-hoc probe builds outside build/)
*.obj
*.exe
*.pdb
*.ilk

# ESP-IDF firmware build artifacts (keep sdkconfig.defaults + idf_component.yml; ignore generated)
firmware/build/
firmware/managed_components/
firmware/sdkconfig
firmware/sdkconfig.old
firmware/dependencies.lock
